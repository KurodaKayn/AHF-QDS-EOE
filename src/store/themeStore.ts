/**
 * Theme store re-export shim for backwards compatibility.
 * Re-exports theme store and types from @/model/theme.
 */
export { useThemeStore } from "@/model/theme";
export type { Theme, ThemeState } from "@/model/theme";
